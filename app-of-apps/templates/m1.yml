apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: m1
  namespace: m1
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.amqstreams.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: m1
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: 'https://github.com/learn-quarkus/quarkus-beginners-helm.git'
    targetRevision: 'main'
    path: m1
    helm:
      valuesObject:
      